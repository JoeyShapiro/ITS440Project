<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ITS440Proj.PageShoppingList"
             Title="Shopping List">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Shopping List!"
                   FontSize="Large"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" 
                TextColor="Red"   
                   />
            <!-- list for shopping list -->
            <ListView x:Name="listShopping" GroupDisplayBinding="{Binding LongName}" GroupShortNameBinding="{Binding ShortName}" IsGroupingEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <!-- each item in the list -->
                        <ViewCell>
                            <ViewCell.ContextActions>
                                <MenuItem Clicked="OnDelete" CommandParameter="{Binding .}" Text="Delete" IsDestructive="True" />
                                <MenuItem Clicked="OnEdit" CommandParameter="{Binding .}" Text="Edit" />
                            </ViewCell.ContextActions>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="1" x:Name="listShopButton" Text="Got" Clicked="listShopButton_Clicked" BackgroundColor="Azure" BorderWidth="1" TextColor="Red" BorderColor="Black" CornerRadius="5" />
                                <Label Grid.Column="2" x:Name="listShopTitle" Text="{Binding Title}" TextColor ="Red"   HorizontalOptions="FillAndExpand" FontSize="Title" />
                                <Label Grid.Column="3" Text="{Binding Description}" TextColor ="Red" HorizontalOptions="FillAndExpand" FontSize="Body" />
                                <Label Grid.Column="4" Text="{Binding Quantity}" TextColor ="Red" HorizontalOptions="FillAndExpand" FontSize="Body" />
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="buttonAdd" Text="Add Item" HorizontalOptions="FillAndExpand" BackgroundColor="Azure" BorderWidth="1" TextColor="Red" BorderColor="Black" CornerRadius="5" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>